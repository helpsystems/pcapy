language: python
python: 2.7
addons:
  apt:
    packages:
      - libpcap-dev
matrix:
  include:
    - os: osx
      language: generic
      install: brew install libpcap
    - os: linux
    - os: linux
      dist: trusty
script:
  - pip install .
  - python setup.py bdist_wheel
